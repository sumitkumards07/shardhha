<style>
.newsletter-section {
  padding: 3rem 0;
  text-align: center;
}
.newsletter-container {
  max-width: 600px;
  margin: 0 auto;
  padding: 0 1rem;
}
.newsletter-title {
  font-family: var(--font-heading);
  font-size: clamp(1.8rem, 4vw, 2.2rem);
  color: var(--color-dark-green);
  margin-bottom: 0.5rem;
}
.newsletter-desc {
  font-family: var(--font-body);
  font-size: 0.95rem;
  color: var(--color-primary-text);
  margin-bottom: 1.5rem;
}
.newsletter-form {
  display: flex;
  gap: 0;
  max-width: 500px;
  margin: 0 auto;
  border: 1px solid var(--color-border);
  border-radius: 50px;
  overflow: hidden;
  background: #FAF9F6;
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
}
.newsletter-form:focus-within {
  border-color: var(--color-dark-green);
  box-shadow: 0 0 0 3px rgba(15, 45, 36, 0.08);
}
.newsletter-input {
  flex: 1;
  padding: 0.9rem 1.5rem;
  border: none !important;
  font-family: var(--font-body);
  font-size: 0.95rem;
  outline: none;
  background: transparent !important;
  border-radius: 0 !important;
}
.newsletter-btn {
  background-color: var(--color-dark-green);
  color: #FAF9F6;
  border: none;
  padding: 0 2rem;
  font-family: var(--font-body);
  font-size: 0.85rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  cursor: pointer;
  transition: background-color 0.3s ease;
  border-radius: 50px;
  margin: 4px;
}
.newsletter-btn:hover {
  background-color: var(--color-muted-gold);
}
.newsletter-feedback {
  margin-top: 1rem;
  padding: 0.75rem 1.5rem;
  border-radius: 50px;
  font-size: 0.9rem;
  display: inline-block;
}
.newsletter-feedback--error {
  background-color: #FFEBEE;
  color: #C62828;
}
.newsletter-feedback--success {
  background-color: #E8F5E9;
  color: #2E7D32;
}

@media (max-width: 480px) {
  .newsletter-section {
    padding: 2rem 0;
  }
  .newsletter-form {
    flex-direction: column;
    border-radius: 20px;
    padding: 4px;
  }
  .newsletter-input {
    text-align: center;
    padding: 0.8rem 1rem;
  }
  .newsletter-btn {
    padding: 0.8rem 1rem;
    width: 100%;
    margin: 0;
    border-radius: 16px;
  }
}
</style>

<section class="newsletter-section">
  <div class="newsletter-container">
    {% if section.settings.title != blank %}
      <h2 class="newsletter-title">{{ section.settings.title }}</h2>
    {% endif %}
    {% if section.settings.description != blank %}
      <p class="newsletter-desc">{{ section.settings.description }}</p>
    {% endif %}

    {% form 'customer', class: 'newsletter-form' %}
      <input type="hidden" name="contact[tags]" value="newsletter">
      <input type="email"
        name="contact[email]"
        class="newsletter-input"
        value="{% if customer %}{{ customer.email }}{% endif %}"
        placeholder="{{ section.settings.placeholder }}"
        aria-label="Email address"
        required>
      <button type="submit" class="newsletter-btn">
        {{ section.settings.button_text }}
      </button>
      
      {%- if form.errors -%}
        <div class="newsletter-feedback newsletter-feedback--error">{{ form.errors.translated_fields['email'] | capitalize }} {{ form.errors.messages['email'] }}</div>
      {%- endif -%}
      {%- if form.posted_successfully? -%}
        <div class="newsletter-feedback newsletter-feedback--success">Thanks for subscribing!</div>
      {%- endif -%}
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Newsletter",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Get Early Access to New Arrivals"
    },
    {
      "type": "text",
      "id": "description",
      "label": "Description",
      "default": "Be the first to know when new kurtis drop. No spam, ever."
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Input Placeholder",
      "default": "E-mail"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Subscribe"
    }
  ],
  "presets": [
    {
      "name": "Newsletter"
    }
  ]
}
{% endschema %}
